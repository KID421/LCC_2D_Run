using UnityEngine;

public class Dog : MonoBehaviour
{
    // 欄位 field (變數)
    // 欄位類型 欄位名稱 (指定 值) 結束
    int jumpCount = 2;
    int jump = 100;
    float speed = 10.5f;
    bool isGround;              // true/false
    string dogName = "KID";
}